/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['cdn.rareblocks.xyz','images.unsplash.com','cdn.sanity.io','source.unsplash.com'],
      },
}

module.exports = nextConfig
